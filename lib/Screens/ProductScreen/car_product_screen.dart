import 'package:flutter/material.dart';

class CarProductScreen extends StatefulWidget {
  const CarProductScreen({Key? key}) : super(key: key);

  @override
  _CarProductScreenState createState() => _CarProductScreenState();
}

class _CarProductScreenState extends State<CarProductScreen> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Commin Soon......")),
    );
  }
}
