import 'package:flutter/material.dart';

class SportsProductScreen extends StatefulWidget {
  const SportsProductScreen({Key? key}) : super(key: key);

  @override
  _SportsProductScreenState createState() => _SportsProductScreenState();
}

class _SportsProductScreenState extends State<SportsProductScreen> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Commin Soon")),
    );
  }
}
